# Banco de dados
DB_NAME=Angel_Origamis
DB_USER=angel
DB_PASS=
DB_HOST=127.0.0.1

# Autenticação
JWT_SECRET=
PORT=3001

# Usado só pelo seed inicial (npm run seed) para criar a conta admin. Não usado em runtime.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Upload de imagens de produtos em produção (aponta para a pasta servida pelo Nginx).
# Deixe em branco em desenvolvimento local.
PRODUCTS_UPLOAD_DIR=
